import React from 'react';
import ReactDOM from 'react-dom';
import App from './app'; // Adjusted to import app.js

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('react')
);